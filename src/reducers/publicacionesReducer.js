//import { TRAER_TODOS, LOADING, ERROR } from "../types/usuariosTypes";

const INITIAL_STATE = {
  publicaciones: [],
  loading: false,
  error: "",
};

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
      
    default:
      return state;
  }
};
